// good name
function sayHello(personName) { // describes the purpose of the function
  console.log(`Hello, ${personName}!`)
}

// bad names
function func(personName) { // does not explain what the function does
  console.log(`Hello, ${personName}!`)
}

function sayhello(personName) { // is hard to read, the second word starts with a small letter
  console.log(`Hello, ${personName}!`)
}
